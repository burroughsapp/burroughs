- content_for(:title) { @location.address }

%p
  %b Latitude:
  = @location.latitude
%p
  %b Longitude:
  = @location.longitude

.form-actions
  = link_to 'Edit', edit_location_path(@location), class: 'btn btn-primary'

