- content_for(:title) { 'New artifact' }

= simple_form_for [@artifactable, @artifact], multipart: true do |f|
  .form-inputs
    = f.input :name
    = f.input :description
    = f.file_field :file
  .form-actions
    = f.button :submit, class: 'btn btn-primary'
